var sql = require("mssql");

module.exports = {
    getExercisesByTargetMuscleGroups: function(targetMuscleGroups) {
        var data = {};
        // TODO

        return data;
    },

    createWorkoutPlanEntry: function(userId) {
        var planId = 0;
        // TODO

        return planId;
    },

    createWorkoutExerciseEntries: function(workoutPlan) {
        // TODO
    }
}